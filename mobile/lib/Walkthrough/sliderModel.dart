class SliderModel {
  // represents the content that would be displayed on the walkthrough screens
  String logoUrl;
  String appName;
  String imageUrl;
  String title;
  String description;

  SliderModel(
      {this.logoUrl,
      this.appName,
      this.imageUrl,
      this.title,
      this.description});
}